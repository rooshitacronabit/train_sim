/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 --instanceall --resolution 1024 --types --output ./GreenGrassPatchModel.tsx --transform ./GreenGrassPatch.gltf 
Files: ./GreenGrassPatch.gltf [21.71KB] > /home/cronabit/Desktop/TreesWithInstance/GreenGrassPatch/GreenGrassPatch-transformed.glb [272.41KB] (-1155%)
*/

import * as THREE from 'three'
import React from 'react'
import { useGLTF, Merged } from '@react-three/drei'
import { GLTF } from 'three-stdlib'

type GLTFResult = GLTF & {
  nodes: {
    Plane003__0: THREE.Mesh
  }
  materials: {
    ['Scene_-_Root']: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

type ContextType = Record<string, React.ForwardRefExoticComponent<JSX.IntrinsicElements['mesh']>>

const context = React.createContext({} as ContextType)

export function Instances({ children, ...props }: JSX.IntrinsicElements['group']) {
  const { nodes } = useGLTF('./trees/GreenGrassPatch-transformed.glb') as GLTFResult
  const instances = React.useMemo(
    () => ({
      Plane: nodes.Plane003__0,
    }),
    [nodes],
  )
  return (
    <Merged meshes={instances} {...props}>
      {(instances: ContextType) => <context.Provider value={instances} children={children} />}
    </Merged>
  )
}

export function Model(props: JSX.IntrinsicElements['group']) {
  const instances = React.useContext(context)
  return (
    <group {...props} dispose={null}>
      <instances.Plane position={[0, 0.206, 0]} rotation={[0, Math.PI / 4, 0]} scale={0.206} />
    </group>
  )
}

useGLTF.preload('./trees/GreenGrassPatch-transformed.glb')
